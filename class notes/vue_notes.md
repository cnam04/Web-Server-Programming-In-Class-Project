## folders
dynamically genrated assets go in /public

views folder -> components used as pages

router-> maps URLs to views

stores -> stores logic




## Things to do later 
reconfigure client with router

add bulma to dependencies manually by editing package.json
add fontawesome


add @import to css files

copy-paste bulma navbar into navbar component

control + space on a component to add import

leave with homeview, aboutview, and app.vue
